

let arr = ["Kharkiv", "Kiev", ["Borispol", "Irpin"], "Odessa", "Lviv", "Dnieper"];
let parent = document.createElement('ul')



/* let timer;
let a = 3;
obr();
function obr(){
    body.innerHTML = a;
    a--;
    if (a<0){
        clearTimeout(timer);
        parent.remove();
        body.innerHTML = ""; // Другого не придумал )
    }
    else{
        timer = setTimeout(obr, 1000);
    } 
   
}
   */